using Godot;

public class FindPhysics
{
    public static void Check(Node node)
    {
        // Just checking how DestructibleObject inherits.
        // It's Node2D, but it might have Area2D or StaticBody2D children.
    }
}
